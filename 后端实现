后端项目这部分，是用TypeScript来做的开发，代码组织上走了MVC（Model-View-Controller）模式；因为前后端分离架构里，View层已经交给了前端去负责，所以后端这边主要就是把Model（数据模型）和Controller（控制器）这两部分给包含进来了。
项目目录的结构是这么去安排的：src/config目录，专门放配置文件，数据库配置和初始化脚本都归在了这里面；src/controllers目录下，放的则是控制器文件，每个控制器会负责去处理一类特定资源的请求；src/middleware目录，存着中间件文件，像认证中间件、错误处理中间件、文件上传中间件这些东西全都被收在了里头；src/routes目录，负责存放路由文件，把URL路径跟控制器方法之间的映射关系给定义了出来；src/types目录，放的是TypeScript的类型定义；src/utils目录呢，把工具函数都集中到了一起，比如JWT处理、密码加密、响应格式化这些；src/app.ts这个文件，就是整个应用的一个入口了，初始化Express应用、配置中间件、注册路由、启动服务器这些事情，全部由它来负责完成。
数据库连接的具体实现，被放在config/database.ts文件里面了，用better-sqlite3库去建起了数据库的连接；这个库提供出来的是同步API，操作数据库的代码因此被简化了不少，连接一旦建好，就去执行了几条PRAGMA语句来给数据库设参数，比如把外键约束给打开了，还设置了WAL模式让并发性能得到了提高，最后把数据库连接对象导了出来，方便别的模块能拿去用。
认证中间件这部分，在middleware/auth.ts文件里把认证中间件函数给定义出来了；中间件会从请求头的Authorization字段里把JWT令牌取出来，然后用jsonwebtoken库去校验令牌的签名和有效期，验证要是通过了，就把用户信息附加到请求对象上面，供后面的控制器去使用，验证没有通过的话，就会返回一个401的错误响应。
控制器的实现，可以用ItemController来当例子，在controllers/itemController.ts文件里，处理失物信息请求的方法都被定义在了里面。getItems这个方法，去处理了获取失物列表的请求，从查询参数里拿到分页和筛选条件，再构造出SQL查询语句，把查到的结果返回去；createItem方法，则处理了发布失物信息的请求，从请求体里获取失物信息，校验了必填字段，随后构造INSERT语句插进数据库，新创建出来的失物信息就被返回了；updateItem和deleteItem这两个方法，分别要去处理更新和删除的请求，一个关键的方面是，用户权限的验证被放在了前面，保证了只有本人才能去操作自己发布出来的那些信息。
错误处理的实现，放在了middleware/errorHandler.ts文件里，一个全局的错误处理中间件被定义了出来；这个中间件会捕获路由处理过程当中抛出的异常，再根据错误的类型去返回相应的错误响应，对于验证错误，返回的是400状态码和详细的错误信息；认证错误呢，返回401状态码；权限错误就返回403；服务器内部错误，则返回500，这种统一起来的错误处理机制，把系统的健壮性和用户体验都提上去了。